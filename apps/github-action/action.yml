name: 'FeatureMap'
description: 'Feature-aware pull request analysis: affected features, risk, tests and mapping drift as a GitHub check run.'
inputs:
  token:
    description: 'GitHub token with checks:write permission (e.g. the GITHUB_TOKEN secret)'
    required: true
runs:
  using: 'node24'
  main: 'dist/index.js'
branding:
  icon: 'activity'
  color: 'blue'
