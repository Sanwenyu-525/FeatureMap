{
  "name": "@featuremap/cli",
  "version": "0.0.1",
  "private": true,
  "description": "FeatureMap command-line interface.",
  "type": "module",
  "bin": {
    "featuremap": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "test": "echo \"no cli tests yet\" && exit 0"
  },
  "dependencies": {
    "@featuremap/analyzer": "workspace:*",
    "@featuremap/core": "workspace:*",
    "@featuremap/db": "workspace:*",
    "@featuremap/scanner": "workspace:*",
    "commander": "^12.1.0",
    "drizzle-orm": "^0.38.3",
    "execa": "^9.5.2",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@featuremap/tsconfig": "workspace:*"
  }
}
