{
  "name": "simple-login-fixture",
  "private": true,
  "version": "0.0.0",
  "description": "Ground-truth fixture 01: single-feature TypeScript login backed by an Express endpoint, with shared infrastructure (logger, http-client) that must not be claimed as feature ownership."
}
