{
  "name": "@featuremap/mcp",
  "version": "0.0.1",
  "private": true,
  "description": "FeatureMap MCP server: bounded feature context for coding agents.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run"
  },
  "dependencies": {
    "@featuremap/core": "workspace:*",
    "@featuremap/db": "workspace:*",
    "@featuremap/pipeline": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "@featuremap/tsconfig": "workspace:*",
    "vitest": "^2.1.8"
  }
}
