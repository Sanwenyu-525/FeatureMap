{
  "name": "@featuremap/core",
  "version": "0.0.1",
  "private": true,
  "description": "FeatureMap domain types and algorithms (framework-agnostic).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@featuremap/tsconfig": "workspace:*",
    "vitest": "^2.1.8"
  }
}
