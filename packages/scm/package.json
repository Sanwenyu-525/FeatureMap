{
  "name": "@featuremap/scm",
  "version": "0.0.1",
  "private": true,
  "description": "SCM transport for PR intelligence: provider abstraction, GitHub REST client, check rendering (Phase 4 / ADR-0006).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run"
  },
  "dependencies": {
    "@featuremap/pipeline": "workspace:*"
  },
  "devDependencies": {
    "@featuremap/analyzer": "workspace:*",
    "@featuremap/db": "workspace:*",
    "@featuremap/tsconfig": "workspace:*",
    "execa": "^9.5.2",
    "vitest": "^2.1.8"
  }
}
